apiVersion: v1
kind: Service
metadata:
  name: podinfo-frontend
  labels:
    app: podinfo-frontend
spec:
  type: ClusterIP
  selector:
    app: podinfo-frontend
  ports:
    - port: 9898
      targetPort: 9898
      protocol: TCP
      name: http
